extends "res://tests/test_case.gd"

const FP := preload("res://src/sim/fp.gd")
const SimConfig := preload("res://src/sim/sim_config.gd")
const SimState := preload("res://src/sim/sim_state.gd")
const Simulation := preload("res://src/sim/simulation.gd")
const Chars := preload("res://src/sim/chars.gd")

const TICKS := 3600

# 2段構えの決定論検証:
# (1) 同一入力2回実行のハッシュ一致 = プロセス内の非決定要素(グローバルRNG等)を検出
# (2) ゴールデンハッシュ = 挙動の意図しない変化を検出。マシンをまたげばfloat差異も
#     いずれ露見する。なお同一プロセス内のfloat混入は(1)では捕まらないため、
#     静的スキャン(test_no_float_in_sim.gd)が併走している
# ゴールデンは全チェックポイント(60tickごと+最終)の合成ハッシュで比較する。
# 最終ハッシュのみだとreset_rallyの状態正規化で途中の物理変化を見逃すため
# 物理を意図的に変更した場合はGOLDEN_COMBINED_HASHを新しい値に更新すること

# 2026-07-25 CPU入力列の変化により更新。実測値(代行環境で全スイート実行)。
# 変更の内訳:
#  1. sim_cpu.gd の三項式型エラー解消。_sweet_jump_plan が常に空配列を返して
#     _decide_positioning が中断していたのが、初めて正常動作するようになった
#  2. サーブ経路での IN_JUMP 保持。ジャンプが18pxのホップから135pxへ戻った
#  3. 打撃tickでの IN_JUMP 保持。打つ瞬間に可変ジャンプが失速していたのを止めた
#  4. 空中打撃の芯判定をこのtickの移動後の位置で行う先読み。
#     simulationは 入力→プレイヤー移動→ヒット判定 の順なので、移動前の座標で
#     判断すると常に1tick遅れて芯を通り過ぎていた
#
# 2026-07-25 (第2回) さらに以下の挙動変更を織り込んで再更新。実測値:
#  5. 地上レシーブの横速度を原作準拠の構造へ。前方固定バイアス(dir*40px/s)を廃し、
#     接触オフセット依存 + 決定論散り + 上限クランプにした。左右で打ち合ったときの
#     不動点(57.1px/s)が消え、CPU同士の無限往復ループが死んだ(「普通」得点 0→3)
#  6. CPU味方の役割分担。カバー距離を cpu_mate_spacing_px 化し、全プリセットに
#     AB_ROLES を付け、近すぎる非レシーバーは打撃入力を出さない
#     (「普通」同士で味方が32px以内にいる時間 28%→3%)
#  7. ネット越え判定の枝漏れ修正。below_top の枝が「越えた」判定を飲み込んでおり、
#     ネット上端より低い高さで横切った球が serve_flight=1 のまま残って
#     誰も触れなくなっていた(人間のアタックサーブがサービスエースになる不具合)
#  8. ネット衝突の掃引判定化。移動後の1点でなく1tickの経路が帯を横切るかで
#     判定するため、2040px/s以上の高速球がネットをすり抜けなくなった
#  9. サーブ球を受け手の初接触までブロック不可にする serve_ball フィールドを
#     SimState へ追加。state_hash が全intフィールドを畳むため、値が0でも
#     フィールドが増えれば全ハッシュがずれる
# 10. 壁反射とネット衝突で ball_attack_kind / ball_guard_damage をクリア。
#     勢いを失った球がアタック球の印を持ち続けなくなった
#
# 2026-07-25 (第3回) アタック速度の引き下げで再更新。実測値。
# 通常アタック 80→50%、ジャストアタック 110→80%。着弾点は変わらず(vxは
# 目標xから逆算されるため)、変わるのは球が届くまでの時間だけ。実測で
# ジャストの最速球が0.18秒→0.23秒、通常が0.23秒→0.28秒になった。
# 他の人に遊んでもらったところ「何もできない」状態だったという実プレイの
# フィードバックが根拠。人間の単純反応は0.20〜0.25秒で、旧ジャストの
# 0.18秒は反応不可能な領域にあった。
#
# 2026-07-25 (第4回) パワーランクによる速度差の試遊のため再更新。実測値。
# 基準を 通常50→33%、ジャスト80→53% へ下げ、POWER倍率[150,125,100,75,50]を
# 掛けたときランクAが従来の速度(通常50/ジャスト80相当)に来るようにした。
# あわせて PIYO の POWER を E、UMA の POWER を A へ振った。
#
# 2026-07-25 (第5回) 実プレイの結果さらに減速。実測値。
# パワーC基準を 通常33→25%、ジャスト53→40% へ。ユーザーが遊んで
# 「拾いやすく反応しやすくなって体験が良くなった、もっと遅くてもいい」と
# 判断したため。これでゲーム最速の球(パワーAのジャスト鋭角)が0.23→0.27秒になり、
# 人間が反応できない球がゲームから無くなる。
#
# 2026-07-25 (第6回) CPUの2件の修正で更新。実測値。
#  1. サーブ経路のジャンプキー保持からIN_ACTION条件を外した。打撃tickだけ
#     IN_JUMPが落ちて可変ジャンプが半減し、予測より低い位置で空振りしていた。
#     ラリー経路は既に条件なしで保持しており、そちらだけ直っていた。
#     実測: CARBY(ジャンプB)のサーブ失敗 88回中24回→0回、TOME(A) 3回→0回
#  2. 自チームのアタック球にCPUが打撃入力を出さないようにした。
#     味方のアタックを触ってトスに化けさせる不具合。
# 2026-07-25 (第7回) ジャストレシーブの構えに入る条件を、水平距離ではなく
# 「今立っている位置で楕円リーチにボールが入るか」に変えたためCPUの入力列が変わった。
# 実測: 最強は見送り62/928(6%)→46/928(4%)、16回中8回以上見送る着弾点は
# [344,364,452]→[344]。強いは90/928(9%)→82/928(8%)、同着弾点は
# [344,348,364,452]→[344,348]。
# 2026-07-25 (第8回) SimStateに3フィールド追加(human_team_mask / rally_seq /
# last_touch_idx)。加えて「相方が人間か」の判定を、操作スロット番号だけでなく
# 実際に人間がいるチームかで行うようにした。CPUだけの敵チームでは片方が
# 相方を人間と誤認して非対称な譲り規則を使っていた。実測値。
# 2026-07-25 (第9回) ラリー単位の役割抽選を導入。ラリー番号とチーム番号から
# 毎回引き直す方式(状態は保存しない)で、そのラリーのアタッカー役をチームに1人だけ
# 決める。原作 VB22.EXE の role_assign(0xBB40) の9通りをそのまま移植した。
# 役は禁止ではなく優先権で、役を持つ相方が会合できないときはもう一人が打てる。
# あわせて「自分が上げたトスは自分で打たない」を相方が人間のチームだけに適用。
# CPUの入力列が変わるためハッシュがずれる。実測値。
# 2026-07-26 コート幅を448→576、ネットを224→288へ広げたため更新。実測値。
# 2026-07-26 (第2回) 原作の待機位置表(0x8406)導入で打球回数カウンタと役マスクを
# SimStateへ追加し、CPUの立ち位置が変わったため更新。実測値。
# 2026-07-26 (第3回) 自陣トスの着弾点を原作へ戻し、3打目の強制を入れたため更新。実測値。
# 2026-07-26 (第4回) 敵陣への返球を固定速度式へ変えたため更新。実測値。
# 2026-07-26 (第5回) 次の2つの挙動変更で更新。実測値。
#  1. スパイクの横速度が打つ位置で2.3倍変わっていたのをやめ、押した横キーごとの
#     固定値にした(コート奥1578px/s・ネット際678px/s → どこでも582px/s)
#  2. CPUがサーブのトスと同時にジャンプして低い打点でしか打てず、球がネットに
#     当たっていたのを、頂点付近で打つよう離陸を待たせた(打点137px → 172px)
# 2026-07-26 (第6回) CPUの反応遅延を原作準拠へ変えたため更新。実測値。
# 原作は打球時に10のカウンタを立てて毎コマ減らし、難易度別のしきい値まで下がるまで
# 思考ごと止める(VB22.EXE 0x809E / 0x0E72)。止まるコマ数は 7/6/4/0 で最強は0。
# 原作のフレームレートを30コマ/秒と仮定して 0.30/0.20/0.10/0秒 とし、
# 本作(60コマ/秒)では 18/12/6/0 コマ。変更前は 24/16/13/12 コマ。
# 2026-07-28 (第7回) #82の未完成実装をHEADへ剥離した緑の足場で再採取。実測値。
# 新規仕様の追加ではなく、ゴールデン基準値の由来訂正である。
# 旧値 1173764543217565771 は、第5回・第6回を未コミットの単一hunkでまとめて
# 更新しており、採取時のtreeまたはcommitが保存されていない
# (`git log --all -S1173764543217565771` は0件。HEADもstashも旧々値のまま)。
# 保存済みの剥離前diffには #82 の部分実装(SALT_AIR_SHOT / _trial_spike_candidate /
# _pick_air_shot_result)が同居していたため、旧値が赤い途中状態を含んでいた
# 可能性を排除できない。
# #102・CPUサーブ打点上昇・CPU反応遅延だけを残し、ゴールデン以外432/432緑を
# 確認した状態の実測値を、新しい追跡可能な基準とする。
# 2026-07-28 (第8回) #88a で SimState へ rng と aitick を追加したため更新。実測値。
# 状態の形が変わればこの値は必ず動く。ゲームプレイ判断は変えていない。
# 440本中、状態ハッシュを固定するこの検査と test_hit_chain_second_golden の
# 2本だけが赤で、残り438本は緑だった。とくに双子検査
# (test_rng_scaffold_keeps_cpu_inputs_and_gameplay_state_seed_independent) が緑で、
# seed を 0x0000 と 0xFFFF に分けた120tickでCPU入力列と全状態が一致している
# =seed差がゲームプレイへ漏れていない。既存の抽選は1つも新状態を読んでいない。
# #88b で抽選の乱数源を切り替えるとき、もう1回だけ更新する予定。
# 2026-07-28 (第9回) #88b-1 で役割抽選を原作方式のステートフル消費へ移したため更新。実測値。
# ラリー開始時にチーム0→チーム1の順で rng = (rng*2+1) & 0xFFFF を2回進め、
# それぞれの % 9 を SimState へ保存する(原作 role_assign 0xBB40 の写し)。
# 役割の決まり方そのものが変わるのでCPUの入力列が動く。分岐表は1文字も変えていない。
# 442本中、状態ハッシュを固定するこの検査と test_hit_chain_second_golden の2本だけが
# 赤で、残り440本は緑。#88b-2 で残りの抽選を切り替えるとき、もう1回更新する予定。
#   旧値 -6955139922794912817
#
# 2026-07-28 (第10回) #88b-2 で残存抽選の乱数源を切り替えたため更新。実測値。
#   旧値 -6955139922794912817 -> 新値 148595592752712713
# 許可抽選(_sweet_ok/_attack_ok)が生の aitick を読むようになり、本作独自の抽選7箇所が
# aitick・actor・用途IDの読み取り専用派生値へ移った。抽選の存廃・確率・発火条件・法・
# 閾値は一つも変えていない。変えたのは乱数源だけ。
# 再採取前 447本中この1本だけが赤、残り446本は緑。再採取後 447 tests, 0 failed。
#
# 第9回の予告と違い、test_hit_chain_second_golden は今回赤にならなかったので触っていない。
# あの検査は _world() が SimState.new() だけを使い Simulation.tick() を通らないため、
# 全過程で tick=0/rng=0/aitick=0 のまま動く。旧 keyed_hash(s.tick=0,..) と
# 新 keyed_hash(aitick=0,..) が同一値になるので、乱数源の変更を原理的に検出できない。
# 代わりに test_refactor_characterization.gd.test_scatter_stream_snapshot が捉えた。
# 2026-07-29 #82でCPU空中打撃候補を実打球速度へ統一し、4政策を実装したため更新。実測値。
# 1・2打目の空中トスも自陣前方へ戻したため、CPU入力列と打球軌道が意図どおり変わる。
#   旧値 148595592752712713 -> 新値 4728804889166836807
# 再採取前482本中この1本だけが赤、残り481本は緑。SCRIPT ERRORは0件。
# コードレビューで空中通常サーブまで自陣トス化する回帰を検出し、サーブだけ敵陣返球へ
# 戻したため再更新。中間値 4728804889166836807 -> 最終値 -7934760329014079569。
# 再採取前483本中この1本だけが赤、残り482本は緑。SCRIPT ERRORは0件。
# 2026-07-29 #91でラリー再開時にtap_dir/tap_tick/dash/pushを消すため更新。実測値。
# 差を生んだのは旧ラリーのダブルタップ受付窓tap_tickだけで、61チェックポイント中
# 別々のreset直後に当たるindex 9/18/47/59だけが変化し、各々の次では再一致した。
#   旧値 -7934760329014079569 -> 新値 5482364282085200630
# 再採取前492本中この1本だけが赤、残り491本は緑。SCRIPT ERRORは0件。
# 2026-07-29 #82: ジャンプサーブ共通化後、ブロック側120px制限を撤去。
# 各段階の独立比較は docs/tasks/82-golden-evidence.md を参照。
# 2026-07-30 味方地上標準トスだけを680px/sへ上げ、トス能力による高さ差を一時無効化。
# 変更前518本中、新仕様に対応する4本だけが赤。同期A/B、3打目返球、レシーブ、
# 空中トス、サーブ、専用設定の隔離は緑。旧値→新値を実測更新。
#   -1365586972767118918 -> -4803537611480018852
# 2026-07-30 横っ飛び用のゼロ初期値3欄を全プレイヤーへ追加。物理と入力列は
# まだ変えず、直列化レイアウトだけが変わったため実測更新。
#   -4803537611480018852 -> -2522542381510534503
# 2026-07-30 ACTIONラッチを全入力消費tickで更新し、予測式の横っ飛び開始・移動と
# 旧地上トス演出用diveの撤去を反映。新しい決定論的状態遷移を全件実行して実測更新。
#   -2522542381510534503 -> 181665681293941071
# 2026-07-30 通常・ジャストレシーブの高度と制御を三段階仕様へ変更:
#   181665681293941071 -> 3152048927258630912
# 2026-08-01 地上トス・レシーブ防御差の再設計:
#   3152048927258630912 -> 5585539103683821495
# 2026-08-01 #70で上+ボタンを通常アタック、前+ボタンを地上・空中ブロックへ変更。
# ジャンプした同tickの上アタックと前ブロックを全入力ランダム走行へ反映し、
# ユーザー承認後に実測更新。同期再現性と専用状態遷移ゴールデンは不変。
#   5585539103683821495 -> 235445783195309349
# 2026-08-01 #70でCPUも残りドライブに応じて上下アタックを使い分け、
# 空中ブロックをチーム相対の前+ボタンへ変更。ユーザー承認後に実測更新。
#   235445783195309349 -> 3274903981563701704
# 2026-08-01 #70のClaudeレビューで、ブロック後の次打撃が古いblock表示を
# 再利用する残留を発見。打撃共通入口で解除し、ユーザー承認後に実測更新。
#   3274903981563701704 -> -811323160986505181
# 2026-08-02 成立したトス・返球・アタックを表示用hit_kindへ記録した。
# 物理値は不変。直列化対象の表示状態が変わるため、ユーザー承認後に実測更新。
#   -811323160986505181 -> 3071785023893461452
const GOLDEN_COMBINED_HASH := 3071785023893461452

func _next_rand(s: int) -> int:
	# xorshift64。乱数も整数のみで作る
	s ^= s << 13
	s ^= s >> 7
	s ^= s << 17
	return s

func _run_once() -> Array[int]:
	# フルゲーム(サーブ→ラリー→得点→再サーブ)を人間2系統のランダム入力で回す。
	# 全入力ビット(0-127)を含み、切替・ヒット・トス照準・CPU相方も検証対象に入る
	var cfg = SimConfig.new()
	var s = SimState.new()
	Simulation.reset_match(s, cfg, 0, Chars.ROSTER, 0, 0)
	var hashes: Array[int] = []
	var rng := 123456789
	for t in TICKS:
		var inputs: Array[int] = []
		for i in 2:
			rng = _next_rand(rng)
			inputs.append(rng & 127)
		Simulation.tick(s, inputs, cfg)
		if t % 60 == 0:
			hashes.append(s.state_hash())
	hashes.append(s.state_hash())
	return hashes

func test_synctest_60_seconds() -> void:
	var a := _run_once()
	var b := _run_once()
	check_eq(a.size(), b.size(), "チェックポイント数が一致")
	var mismatch := -1
	for i in a.size():
		if a[i] != b[i]:
			mismatch = i
			break
	check_eq(mismatch, -1, "デシンクなし(検出indexは-1)")

func _combined(hashes: Array[int]) -> int:
	# チェックポイント群をFNV-1a 64bitで1本に畳む(state_hashと同じ方式)
	var h := -3750763034362895579
	for v in hashes:
		for i in 8:
			h ^= (v >> (i * 8)) & 0xFF
			h *= 1099511628211
	return h

func test_golden_hash_regression() -> void:
	var a := _run_once()
	check_eq(a.size(), 61, "チェックポイント数(60tickごと+最終)")
	check_eq(_combined(a), GOLDEN_COMBINED_HASH,
		"合成ゴールデンハッシュ一致(物理を意図的に変えた場合のみ更新する)")

func _run_endgame() -> Array[int]:
	# 3600tickの主走行はGAME_OVERに到達しないため、勝敗決定(デュース2点差)と
	# GAME_OVERフェーズの決定論はこの終盤戦シナリオで別途検証する
	var cfg = SimConfig.new()
	var s = SimState.new()
	Simulation.reset_match(s, cfg, 0, Chars.ROSTER, 0, 0)
	# 14-0開始: CPU同士は点の取り合いが交互に近く、僅差デュースだと決着しないため
	# 大差から確実に勝利遷移へ到達させる(左の1点で15点・2点差以上が確定する)
	s.score_l = cfg.points_to_win - 1
	var out: Array[int] = []
	var rng := 987654321
	for t in 3600:
		var inputs: Array[int] = []
		for i in 2:
			rng = _next_rand(rng)
			inputs.append(rng & 127)
		Simulation.tick(s, inputs, cfg)
		if t % 60 == 0:
			out.append(s.state_hash())
	out.append(s.state_hash())
	out.append(s.phase)
	out.append(s.winner)
	return out

func test_endgame_reaches_game_over_deterministically() -> void:
	var a := _run_endgame()
	check_eq(a[a.size() - 2], SimState.PHASE_GAME_OVER, "3600tick以内に勝敗が付く")
	check(a[a.size() - 1] >= 0, "勝者が確定している")
	var b := _run_endgame()
	var mismatch := -1
	for i in a.size():
		if a[i] != b[i]:
			mismatch = i
			break
	check_eq(mismatch, -1, "終盤戦デシンクなし(検出indexは-1)")
