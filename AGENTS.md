# Animal Spike Codex 運用規則

PC-98版フリーゲーム「VOLLEY BALL 2on2」のリメイク。

## 資料と役割

- `docs/project-memory.md` は課題に必要な場合だけ読み、実装元の承認済み設計書は全文を読む。
- 規則の理由と事故記録は `docs/rule-history.md` を、規則改訂時または同種事故の調査時に読む。
- Codex は調査、設計書作成、実装、検証、コミットを担当する。
- 独自仕様、原作からの変更、決着済み判断の変更はユーザーが決める。
- 複雑な処理、根拠が弱い判断、重要な変更では、Codex は仕様書、調査結果、実装を Claude Code に積極的に相談・レビュー依頼し、回答を証拠とコードに照らして最終判断する。
- Claude Code への相談は必須の承認手続きではなく、開発の主体は Codex とする。
- 承認済みの仕様・合格条件を変えず、実測で空振りが確認された検査を本来の検出能力へ是正する変更は、Codex が根拠を確認したうえで実装上の修正として扱う。

## 設計と停止

- 契約の不足・矛盾、コードとの不一致、範囲拡大、契約効果の不成立を見つけた時点で停止する。Codex が調査し、技術的な確認は必要に応じて Claude Code へ相談し、仕様判断はユーザーへ確認する。
- 契約と異なる方式を提案する場合は、実装を止めて根拠と影響を報告する。
- ゲーム値は契約、承認済み設計書、または参照先コードから取得し、暫定値は一つの名前付き定義へ置く。
- 設計書には発火条件、優先順位、状態遷移、式、例外、守る不変条件、検証方法、合格条件を明記する。
- 原作由来と本作独自の判断を分け、定数と式は正本となるコードを参照する。
- 未決事項は識別可能な停止条件として記録し、各決定を検索可能な名前で示す。

## 実装と検証

- 実装前に設計書の行数と SHA-256 を承認値と照合し、一致した版から作業する。差異は停止して報告する。
- 依頼された範囲だけを変更し、関連改善は発見として報告する。
- 固定値・配列・ハッシュの変化は回帰警報として停止し、実測結果を報告する。
- 本番と同じ補助関数で期待値を再計算する検査は自己一致の可能性を報告し、独立した根拠を求める。
- Codex は正規入口 `run_tests.ps1` で全件検証する。実行できない場合は理由と未実行項目を明記する。
- 設計書の報告では行数、SHA-256、変更全件を示す。実装完了時は変更全件、実差分、検証結果、未検証事項を報告する。
- 指示や指摘の正誤は証拠で判定し、誤りを認める場合も異議を示す場合も根拠を添える。
