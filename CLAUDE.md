# Animal Spike Claude Code instructions

Before starting work, read `docs/project-memory.md` and the relevant files in
`docs/superpowers/specs/`.

This project is developed alternately with Claude Code and Codex. They are not
run at the same time. Treat existing uncommitted changes as work from the other
agent or the user: inspect them, preserve them, and continue from the documented
state instead of reverting or duplicating the work.
