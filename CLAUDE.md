# Animal Spike Claude Code 運用規則

PC-98版フリーゲーム「VOLLEY BALL 2on2」のリメイク。原作解析は `C:\work\PC98\vb2211-analysis\` にある。

## 資料

- 現在の課題は `docs/remaining-tasks.md` を正本とし、`docs/project-memory.md`、関連設計書、バックログ、履歴は課題に必要な範囲だけ読む。
- 設計書の照会は該当箇所に絞り、承認する設計書は全文を読む。
- 規則の理由と事故記録は `docs/rule-history.md` を、規則改訂時または同種事故の調査時に読む。

## 決定と分担

- 独自仕様、原作からの変更、決着済み判断の変更は、ユーザーの承認で確定する。
- Claude Code は事実と選択肢を整理し、Codex の査読を受けてから判断案をユーザーへ示す。
- 作業は「方針決定 → Codex が設計書作成 → Claude Code が全文査読・承認・固定 → Codex が実装 → Claude Code が検証・コミット」の順で進める。
- 契約の不足・矛盾、コードとの不一致、範囲拡大、契約効果の不成立を見つけた時点で停止し、Codex またはユーザーへ相談する。
- 承認時は設計書の行数と SHA-256 を固定し、実装完了まで同じ版を維持する。改訂時は実装を停止し、再承認と再固定を行う。

## Codex の起動

- 読む範囲を依頼内に絞り、原則として `--effort high` を使う。
- ジョブ全体に作成・編集が含まれる場合は開始時から `--write` を付け、調査・査読だけのジョブは読み取り権限で起動する。
- `--background` で起動するときは同時に監視処理を起動し、`job.status` が `running` を離れた時点で結果を確認する。
- Codex の稼働中は対象作業ツリーを固定し、実験と並行作業は別の worktree で行う。

## 原作準拠

- 原作に値や仕組みがある場合はそれを既定として採用し、コード上の根拠を示す。
- 原作外の仕様とプレイヤー体験を変える案は、差分と効果を示してユーザーが決める。
- リスクは失敗時に赤くなる検査で覆い、ゲーム挙動の根拠は原作事実とユーザー決定に置く。

## 検証

- 実際の `git diff`、正規入口の全件テスト、ビルドまたは実行結果を確認し、未実行項目を明記する。
- テストの正規入口は `run_tests.ps1` の終了コードとし、要約行と `SCRIPT ERROR` の検査結果も照合する。
- 証拠は環境状態、ツール記録、作業報告の順で評価する。
- 固定値・配列・ハッシュの変化は発見として停止し、到達原因を確認する。再取得は他が緑のツリーで一つの理由ごとに行う。
- コミットは Claude Code が検証後に行い、原因、変更した固定値、テスト件数、失敗件数を記録する。
