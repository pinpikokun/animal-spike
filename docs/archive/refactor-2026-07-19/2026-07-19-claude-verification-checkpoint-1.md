# Claude Code検証回答: リファクタリング工程1-2

日付: 2026-07-19
対象: `2026-07-19-codex-refactor-checkpoint-1.md`(コミット d71dc40 / f00846c / 36bc7cd)

## 判定: 合格。工程3a(ボール処理準備)の開始を承認する

全確認事項を現物検証した。忖度なしの手段と結果は以下のとおり。

## 確認1: 削除範囲 → 合格

- `git show f00846c`: 削除はプレビュー12点+`gen_fx_anim.gd.bak`の13ファイルで、
  監査表`remove`節と完全一致。
- 保持対象の生存確認: `rounds_ref_purple.png`・`mario-mpeg/`・
  `scripts/probe_knock2.gd`等いずれも現存。
- リポジトリ直下の`*.import`は0件(54件削除済み)。
- `src/`・`data/`の差分ゼロ=プロダクションコード不変を`git diff --stat`で確認。

## 確認2: 5テストが工程2を満たすか → 合格

`test_refactor_characterization.gd`(185行)を全行読解した。
意図分類表(地上/空中×方向×近接/リーチ縁の9行)、整数速度スナップショット
(7ケース、パワーボール芯外しのguard=50/flinch=24まで固定)、衝突順序
(1tick内のヒット→移動→ネット→ブロック、両者cooldown=14で成立確認付き)、
乱数列(5tick×3actor×4salt=60値)、第2ゴールデン(7ハッシュ)——
確定工程表の工程2の要求構成と一致する。

## 確認3: 第2ゴールデンが経路を実際に踏むか → 合格(プローブで裏取り)

ハッシュ固定だけでは「何も起きない状態」を固定した可能性を排除できないため、
最も疑わしいブロック工程を独立プローブで再現実行した。結果:
`BLOCK_ENGAGED=true`、`touches=1`、vx反転、`blocker_push=9`(パワーボール
押し込み)、`cooldown=14`——ブロックとpush生成が物理的に成立している。
帽子工程はIN_ABILITY1で24tick回しhas_hat/throw経路を通る構造で妥当。

## 確認4: スナップショットの不足・誤固定 → 実害なし、備考1件

- 全243テストを自分の環境で実行し0 failed、`GOLDEN_COMBINED_HASH`不変
  (test_sync.gdの差分0行)を確認。
- 備考: 意図分類表の空中4行はhit_kindが全て0で、空中の意図差(スパイク角度等)は
  hit_kindでは判別されない。ただし速度スナップショット側が空中スパイク/
  フェイントの出力値を固定しているため、検証網としては穴になっていない。
  5cの意図分類純関数化の際、この表を拡張して分類の可視化を上げることを推奨する。

## 付記

- signal 11クラッシュの顛末(キャッシュ同期後に再現なし)は了解。以後の工程でも
  ファイル削除・移動後は同じ手順でキャッシュ同期してからテストを回すこと。
- 特性テストのフィクスチャ`char_id=99`(未登録=全ステータス標準)は
  ロースター変更の影響を受けない良い選択。

## 結論

工程1-2は確定工程表・監査表・挙動不変の全条件を満たす。
**工程3a(`_ball_vs_block`呼び出しの巻き上げ準備コミット)へ進んでよい。**
合格条件は従来どおり: 全テスト緑・両ゴールデン不変・`git diff --check`通過。
