# 4. #91 ラリー再開で消えない状態＋ネット経路の設定検査漏れ

**中身**: `reset_rally` が stun / burn / dive / brake など多くをクリアする一方、
**push（吹っ飛び速度）/ dash / tap_dir / tap_tick をクリアしていない。**
得点直後の次のラリー開始時に、前のラリーの吹っ飛びが残って動き出す。
あわせて `src/net/sim_root.gd` だけが `SimConfig` の妥当性検査を省いている
（表示層の2経路は検査している）。

**根拠**: 実測。`simulation.gd:403-425` に push / dash / tap_dir の文字列が1つも出てこないことを確認。

**なぜP0か**: 全試合に影響する実バグで、ネット対戦とは無関係。修正は小さい。
放置すると「たまに動きがおかしい」という再現しにくい症状として残り続ける。
