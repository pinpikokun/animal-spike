# 4. 【完了】#91 ラリー再開で消えない状態＋ネット経路の設定検査漏れ

**状態**: 2026-07-29 完了。

**中身**: `reset_rally` が stun / burn / dive / brake など多くをクリアする一方、
**push（吹っ飛び速度）/ dash / tap_dir / tap_tick をクリアしていない。**
得点直後の次のラリー開始時に、前のラリーの吹っ飛びが残って動き出す。
あわせて `src/net/sim_root.gd` だけが `SimConfig` の妥当性検査を省いている
（表示層の2経路は検査している）。

**根拠**: 実測。`simulation.gd:403-425` に push / dash / tap_dir の文字列が1つも出てこないことを確認。

**なぜP0か**: 全試合に影響する実バグで、ネット対戦とは無関係。修正は小さい。
放置すると「たまに動きがおかしい」という再現しにくい症状として残り続ける。

## 完了内容

- `reset_rally` が全選手の `tap_dir` / `tap_tick` / `dash` / `push` を消す。
- 非サーバーの位置、速度、`hit_cooldown` は従来どおり維持する。
- ラリーをまたいだ同方向の最初の1タップが誤ダッシュにならない対照付き検査を追加した。
- `SimRoot.setup()` が `SimConfig.valid` を検査し、無効時は state を作らず失敗を返す。
- `NetMatch` は設定失敗時に同期・後続ノード生成へ進まず、processを止めてHUD生成前のnull参照も防ぐ。

設計書: `docs/superpowers/specs/2026-07-29-91-rally-reset-config-validation-design.md`
（204行、SHA-256 `9c8acdee2d4c4823e1b765f15a87e7e2bfb8e52b4ee1618a1f4a024cdc1779c5`）

## 固定値の実測

四状態クリアにより合成ゴールデンが変化したため停止して切り分けた。
差の原因は `tap_tick` だけで、61チェックポイント中、別々のラリー再開直後4点だけが変化し、
各々の次の点と最終点は再一致した。CodexとClaude Codeの再承認後、
`GOLDEN_COMBINED_HASH` だけを
`-7934760329014079569` から `5482364282085200630` へ更新した。
他の固定値は不変だった。

## 検証

- `run_tests.ps1`: 493 tests, 0 failed、SCRIPT ERROR 0、終了コード0
- `scripts/run_net_test.ps1 -VerifyBootSeed`: BOOT SEED VERIFY PASSED、終了コード0
- Claude Code最終レビュー: P0/P1指摘なし、完了OK
- コミット: `2df2803`、`b960a2b`、`06fc6b0`、`de23860`
