# 0. 【完了】足場作り（2026-07-28 コミット `8db6eff`）

**433 tests, 0 failed。プッシュ済み。作業ツリーはクリーン。**

`#86` → `#88` で決定論乱数の土台に触るため、**未完成の #82 を足場から外して
緑の基準点を作る**のが目的だった。設計書は
`docs/superpowers/specs/2026-07-27-scaffold-isolate-82.md`（238行、Claude Code が
全文査読して承認・ピン留め）。

## 入ったもの

1. **#102 スパイクの横速度を固定にした**。打つ位置で2.3倍変わっていた
   （コート奥1578px/s・ネット際678px/s）のを、押した横キーごとの固定値
   （どこでも582px/s）に。原作 `0x6115` の空中アタック打球式は3方向とも固定値で、
   打点の位置を一切見ていない
2. **CPUサーブの打点を上げた**。トスと同時に跳んでいたのを、芯で捉えられる
   最も高い位置まで離陸を待たせた（打点137→172px）
3. **CPUの反応遅延を原作準拠にした**。24/16/13/12 → 18/12/6/0コマ
   （`0x809E` / `0x0E72`、原作の止まるコマ数7/6/4/0。最強が0＝打った瞬間に反応）
4. 書類整理（タスクを `docs/tasks/<番号>.md` へ分割、完了仕様書を `docs/archive/` へ）

## 外したもの

**#82（CPUの打ち方の選び方）の実装とテスト。** `8db6eff` の直前の未コミット状態に
実装が残っていたが、**再設計するので復元しない**。合意済みの確定仕様は
`docs/tasks/82.md`、着手は #88 の後。

## 赤6本の顛末（すべて決着）

| 赤だったテスト | 結末 |
|---|---|
| `test_max_cpu_uses_attack_serve` | **緑に戻った**（#82 が壊していた） |
| `test_strong_cpu_uses_attack_serve_on_successful_profile_roll` | **緑に戻った**（同上） |
| `test_cpu_jump_serve_reaches_front_and_back_court` | #82 のテストとして封印 |
| `test_trial_shot_validation_05_blocker_reacts_to_deep_attacker` | ファイルごと封印 |
| `test_trial_shot_validation_18_block_positioning_and_priority_unchanged` | ファイルごと封印 |
| `test_golden_hash_regression` | 第7回として張り替え（下記） |

**ブロック2本の赤は実装のバグではなくテストの前提バグだった**（2026-07-27 実測）。
テストの共通土台 `_world()` が `s.players[team * 2]`（＝ブロッカー本人）を
`on_ground = 0` に置いており、**空中のまま `IN_JUMP` を期待していた**。
`_decide_block` は空中では `IN_ACTION | IN_UP`（ブロック動作そのもの）を返すのが
正しく、`IN_JUMP` は地上のときだけ。地上に戻せば2つの期待を両方満たす。
当初「120px条件の撤去による副作用」と書いたのは**誤り**。**#82 の再設計時に直す。**

## ゴールデンハッシュ第7回の経緯（再発防止のため残す）

`GOLDEN_COMBINED_HASH` を `1173764543217565771` → `6286455164936831101` へ。

**旧値は基準値として失効していた。** 第5回・第6回の更新が**未コミットの単一hunkで
まとめて書かれ**、採取時の tree も commit も存在しない（`git log --all -S` が0件。
HEAD も stash も旧々値のまま）。保存されていた剥離前 diff には #82 の部分実装
（`SALT_AIR_SHOT` / `_trial_spike_candidate` / `_pick_air_shot_result`）が同居して
おり、**旧値が赤い途中状態を含んでいた可能性を排除できない。**

剥離が正しいことは別に裏付けた。**HEAD を別 worktree に取り出して回すと 427/427 全緑**、
現在は 433本中ゴールデン以外の432本が緑。**剥離漏れなら他のテストも赤くなる。**

**この事故を繰り返さないための規則を `CLAUDE.md` と `AGENTS.md` へ常設した。**

## この作業で踏んだ罠

- **帰属の判定を1件間違えた。** `hit_resolver.gd` の `spike_target_x()` と定数2つの
  削除を #102 由来と誤認したが、実際は **#82 が「使う人がいなくなったから」消した**もの。
  HEAD の `_pick_air_shot` がこれを呼ぶため、剥離した瞬間に**コンパイルエラー**で
  全テストが落ちた。復活させたら赤が6本→1本に落ちた。
  **同じ関数を消した理由が2つの仕事にまたがるときは、呼び出し側から辿ること。**
- **Codex はこの環境でフルテストを完走できない**（`run_tests.ps1` が120秒の
  コマンドタイムアウトを超え、再試行も権限で拒否される）。
  **テストの実行と判定は Claude Code が行う。** Codex にはコード変更だけを頼む。
- Codex と Claude Code が同時にテストを回して **Godot が3プロセス競合**した。
  片方だけが回すこと。
