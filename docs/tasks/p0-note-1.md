# 0. 【未コミット・作業中】2026-07-26〜27 の変更（5件目が難航中）

**コミットしていない。テストは456本中6本が赤（2026-07-27 実測）。** 次の作業（#82）が
終わってからまとめてコミットする。内容:

1. **スパイクの横速度を固定にした**（#102）。打つ位置で2.3倍変わっていたのを、
   押した横キーだけで決まる固定値にした。実測でコート奥1578→どこでも582px/s。
   設計書 `2026-07-26-spike-fixed-velocity-design.md`
2. **CPUのサーブの打点を上げた**。トスと同時に跳んでいたのを、芯で捉えられる
   最も高い位置まで待たせた。実測で打点137→172px、ネット越え0/24→24/24
3. **サーブの打球を候補から選ぶ仕組みを入れた**。ただし
   **「相手から一番遠い所を選ぶ」が選び方の本体になっており、原作に無い作り。
   #82 で選び方の1つへ格下げする**
4. **CPUの反応遅延を原作準拠にした**。原作は打球時に10のカウンタを立てて毎コマ減らし、
   難易度別のしきい値まで下がるまで思考ごと止める（`0x809E`/`0x0E72`、止まるコマ数
   7/6/4/**0**）。原作のフレームレートを30コマ/秒と仮定して
   **0.30 / 0.20 / 0.10 / 0秒**（本作は60コマ/秒なので18/12/6/0コマ）へ。
   **最強が0=打った瞬間に反応するのは原作どおり。** 変更前は24/16/13/12コマで、
   「0コマ反応はずるい」という本作独自の判断だった

5. **#82 CPUの打ち方の選び方（実装したが動いていない。2026-07-27 調査中）**

**現状**: 実装は入ったが**サーブが機能していない。** 実測で、乱数の出目が1〜4すべて
出ているにもかかわらず、**12回とも同じ球（上向きのトス）しか出ない。**

**原因（Codex調査で判明）**: サーブの打点からは**①手前・②中央・③奥のどれも
「相手コートに入る」と判定されず**、安全弁が働いて全部トスへ落ちている。
出目の仕組み自体は正常に動いている。

**未確定**: 「本当に届かない」のか「実際は届くのに、試し打ちの見積もりが厳しすぎて
無効と判定している」のか。**Codexが調査中**（2026-07-27時点）。

原作のサーブは専用の打球式を持ち、**試し打ちの値と実サーブの値が一致している**
（＝原作のサーブには見積もりのずれが無い）。本作は「サーブもラリーと同じ比率でずらす」
と決めたため、**原作には無いずれをサーブに持ち込んでいる可能性がある。**

**赤いテスト6本の内訳（2026-07-27 実測）**: 3系統ある。**サーブの話だけだと思って
着手すると残り3本に足をすくわれる。**

- **サーブが1種類しか出ない（3本）** — 上の「現状」で書いた本命。
  - `test_cpu.gd.test_cpu_jump_serve_reaches_front_and_back_court`
  - `test_cpu_offense_receive.gd.test_strong_cpu_uses_attack_serve_on_successful_profile_roll`
  - `test_cpu_offense_receive.gd.test_max_cpu_uses_attack_serve`
- **ブロック側（2本）** — **原因確定（2026-07-27 実測）。実装は正常で、テストの前提バグ。**
  - `test_cpu_trial_shot.gd.test_trial_shot_validation_05_blocker_reacts_to_deep_attacker`
  - `test_cpu_trial_shot.gd.test_trial_shot_validation_18_block_positioning_and_priority_unchanged`

  **当初「ネットから120px以内でないと打たない代用品を撤去した副作用」と書いたが誤り。**
  真因は、テストの共通土台 `_world()` が `s.players[team * 2]` を `on_ground = 0`
  （空中）に設定しており、**ブロッカー本人が空中のまま `IN_JUMP` を期待している**こと。
  `_decide_block` は空中では `IN_ACTION | IN_UP`（＝ブロック動作そのもの）を返すのが
  正しい実装で、`IN_JUMP` は地上のときだけ返す。

  実測（`tests/zz_probe_block.gd`）:
  ```
  検証5  空中のまま: _decide_block -> ACTION UP   （テストはJUMPを期待して落ちる）
  検証5  地上に戻す: _decide_block -> JUMP UP     （期待どおり）
  検証18 空中のまま: jump0 -> ACTION UP / jump1 -> (none)
  検証18 地上に戻す: jump0 -> JUMP UP  / jump1 -> (none)  ← 2つの期待を両方満たす
  ```
  役の前提（`_is_rally_blocker`）も、ネット柱までの距離24px条件も、打点圏の条件も
  すべて通っている。**修正はテスト側で blocker を地上に戻す1〜2行。**
- **合成ゴールデンハッシュ（1本）** — `test_sync.gd.test_golden_hash_regression`。
  物理を意図的に変えたときだけ更新する見張り。#82 の決着後に更新する。
  **#82 単独の影響か、#102（スパイク横速度固定）込みかは未確認。**

数字の見張り2本は実測で更新済み。
