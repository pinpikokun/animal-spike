# 6. #92 死んだ設定キー13件・死んだ表示コード・休眠特性13件の棚卸し

**中身**: `rules.json` にあり読み込まれているのに一度も使われない設定キーが13件。
呼ばれない描画関数が複数（`_draw_one_shots` / `_draw_wall_ripple` など）。
「廃止」とコメントに書いてあるのに実装が残っている演出。
名前だけで実装ゼロの特性13件（これは仕様どおりの予約）。
特性の背反検証関数がどこからも呼ばれていない。

**根拠**: 実測。13キーのうち12キーが src 内で参照0件、
`_draw_one_shots` と `_draw_wall_ripple` が参照0件であることを確認。

**なぜP4の6位か**: 実害は無い。ただし「あるつもりで無い」機能が積み上がると、
設定を触っても効かないという形で時間を溶かす。
