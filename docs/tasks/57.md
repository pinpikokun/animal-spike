# #57 原作8キャラのスプライト動作対応

**状態**: 実装完了。原作8キャラはキャラクター選択から使用できる。

## 完了内容

- 全24セルを `00_sprite-sample/スプライトセル対応表.txt` の動作へ登録した。
- 原作8キャラ全員のセル番号、順番、保持tickを自動テストで固定した。
- ピヨの泡はセル4、他7キャラの泡はセル20へ分けた。
- ブロック、地上・空中トス、地上返球、アタック、レシーブ、横っ飛びを正しいアニメへ接続した。
- サーブのセルフトスを含め、成立した動作が古い表示種別を再利用しないようにした。
- 感電、泡、羽ばたき、飛行静止、特殊落下も、アニメ名、セル列、保持tick、代役、全8キャラのテストまで完成した。

## 承認設計

- 設計書: `docs/superpowers/specs/2026-08-02-original-sprite-action-mapping-design.md`
- 改行数: 147
- SHA-256: `6cab4115dc0dde490259fba59f917d0144b7f498228165d7407767cd267c9428`

## 検証

- 正規入口 `run_tests.ps1`: 582件合格、失敗0件、SCRIPT ERROR 0件。
- 固定ハッシュの変更は、物理値ではなく表示用 `hit_kind` の正常化によるものと確認し、ユーザー承認後に更新した。
- Claude Codeレビューでサーブのセルフトスに表示種別の残留を発見し、再現テスト追加後に修正した。
- 再レビューで重大問題なし。追加指摘は既存の地上レシーブ選択テストと承認記録で満たしていることをコードで確認した。

## 将来タスクに残すもの

感電、泡、飛行、特殊落下はゲーム状態が未実装である。将来残る作業は、その状態の発火条件と終了条件から、今回完成した `shock`、`bubble`、`fly`、`fly_hover`、`fall_special` を選ぶ接続だけとする。セル調査、画像切り出し、コマ順、保持tick、アニメ登録は本タスクへ戻さない。

## 試遊確認

自動検証は完了した。原作8キャラのアタック、トス、レシーブ、ブロック、横っ飛び、被弾、気絶、炎上、勝利の見た目と操作感はユーザー試遊で最終確認する。
