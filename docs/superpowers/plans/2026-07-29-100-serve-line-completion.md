# #100 サーブ線14px完了計画

> **実行条件:** Codex が実装し、各判断と最終差分を Claude Code が査読する。

**目的:** 承認済み設計の未達1件である `serve_line_px` 31→14を、通常サーブ左右2件とアタックサーブ726件の実弾道検査を通して完了する。

**実装元:** `docs/superpowers/specs/2026-07-26-return-over-net-design.md` 437行、SHA-256 `ef107cab6fb6a21f1d30bb951f47c6b359b30358b5fe35ae370a67345102c178`。

**範囲:** `data/rules.json` の1値、サーブ回帰検査、同期ゴールデン、#100の完了記録だけ。打点、照準、重力、アタック・スパイク・ブロックの速度、乱数、プレイヤー移動は変えない。

## Task 1: 実弾道の回帰検査を先に作る

**Files:**
- Modify: `tests/unit/test_cpu.gd`

- [ ] `_cpu_jump_serve_result` を、予測関数 `_clears_net()` の結果ではなく、`Simulation.tick()` 後に `serve_flight` が1→0となった最初の実通過を返すヘルパーへ変える。
- [ ] 発射後は最大300tickで打ち切り、1→0を観測できなければ空結果を返して必ずFAILにする。未通過をskipや無限待機にしない。
- [ ] テスト側で通過前フレームの座標を保持し、通過フレームの前後座標からネット中央の球中心yを整数補間して、`net_top_y - ball_radius` 以下を合格とする。
- [ ] サーバーのCPUプロファイルを引数で固定し、通常は `PRESET_NORMAL` かつ `ball_attack_kind == NONE`、アタックは `PRESET_MAX` かつ `ball_attack_kind != NONE` を検査する。条件不一致はskipせずFAILにする。
- [ ] 通常サーブは左右2件、アタックサーブは左右2×得点11×11×受け手配置3=726件を実走する。
- [ ] 得点次元の根拠は `SimCpu._serve_target()` の `score_l` / `score_r` から照準 `aim` / `pow_pct` への直接経路としてコメントに残す。
- [ ] 正規入口 `powershell -NoProfile -ExecutionPolicy Bypass -File run_tests.ps1` を31pxのまま実行する。これは安全検査の先行追加と変更前実測であり、既存挙動を変えるREDではない。
- [ ] 変異検査として `serve_line_px` を一時的に0へ変え、正規入口で新しいサーブ実通過検査が赤になることを確認・記録する。直後に31へ戻し、差分に変異を残さない。

## Task 2: 承認済みの設定値を14へ切り替える

**Files:**
- Modify: `data/rules.json`
- Modify: `tests/unit/test_config.gd`

- [ ] `serve_line_px` を31から14へ変更する。
- [ ] `test_config.gd` の31固定検査と「後で14」の陳腐化コメントを削除する。値そのものを固定しないことは承認設計6.3.1の明示契約であり、左右対称配置と照準clampは `test_rally.gd` が検査済み。
- [ ] 正規入口を実行する。サーブ検査が1件でも赤なら、打点・照準・重力などで直さず停止する。
- [ ] サーブ検査以外が赤なら原因を分離する。同期ゴールデンだけが赤なら、理由が31→14の1件だけであることを確認して実測値へ1回だけ更新する。

この変更は設定データだけであり、承認設計が値固定テストを禁じているため、通常の「新テストをREDにする」対象外とする。その代わり、変更前後で同一728件の実弾道検査を通し、設定の安全性を独立に確認する。この例外は実装前に Claude Code の承認を得る。

## Task 3: 検証、査読、完了記録

**Files:**
- Modify: `tests/unit/test_sync.gd`（同期ゴールデンだけが意図どおり赤の場合のみ）
- Modify: `docs/tasks/100.md`
- Modify: `docs/remaining-tasks.md`

- [ ] 同期ゴールデンを更新する場合、旧値、新値、理由1件、他検査が緑だった事実をコメントへ記録する。
- [ ] 正規入口の終了コード0、唯一の件数要約、failed 0、`SCRIPT ERROR summary: 0 occurrence(s)` を確認する。
- [ ] 同期ゴールデンを更新した場合は正規入口を2回連続で通し、件数と結果が一致することを確認する。
- [ ] `git diff --check` と差分を確認し、Claude Code に設計適合性・検査能力・範囲逸脱・完了可否を査読してもらう。
- [ ] #100を完了へ更新し、残タスク表から現役行を除く。文書更新後に正規入口を再実行する。
- [ ] 実装と完了文書を分けてコミットし、作業ツリーがクリーンであることを確認する。

## 停止条件

- 14pxで通常2件またはアタック726件の実通過・上端クリアが1件でも失敗する。
- 発射後300tick以内に `serve_flight` 1→0を観測できない組み合わせが1件でもある。
- 同期ゴールデン以外の既存検査が、説明できない形で変化する。
- `serve_line_px` 以外のゲーム値、速度式、乱数、プレイヤー移動を変える必要が出る。
- 承認済み設計437行のSHA-256が実装前確認値と一致しない。
