# Claude Code検証依頼: リファクタリング工程1-2

日付: 2026-07-19

## 対象コミット

- `d71dc40`: 不要ファイル監査表
- `f00846c`: ユーザー承認済み生成物の削除
- `36bc7cd`: 抽出前の特性テスト追加

## 工程1

`2026-07-19-unused-files-audit.md` の `remove` だけを削除した。

- Git管理中の生成プレビュー12点
- `scripts/gen_fx_anim.gd.bak`
- Git管理外のリポジトリ直下 `*.import` 54点

`confirm` と `keep` は削除していない。特に `rounds_ref_purple.png`、`mario-mpeg/`、3本の物理診断プローブ、原作キャラクター資料は保持した。

削除直後、Godotのファイルシステムキャッシュが古い状態のまま通常ヘッドレス起動され、一度signal 11でクラッシュした。ヘッドレスeditor scanでプロジェクトキャッシュを同期した後は再現せず、その後の全テストとコミットフックはいずれも成功している。プロダクションコードの変更はない。

## 工程2

`tests/unit/test_refactor_characterization.gd` を追加した。プロダクションコードは変更していない。

固定した経路:

1. 地上・空中、方向入力、近接・リーチ縁による意図分類
2. 静止球・強球・パワーボール、ジャスト・芯外し、主要入力の整数出力速度
3. 同tickのヒット、ボール移動、ネット、ブロックの衝突順序
4. 固定tick、actor、saltに対する `_scatter` 60値
5. ジャストアタック、パワーボール芯外し、ガード破壊、ブロック、帽子投げを踏む第2ゴールデン

テストは、空の期待値で意図的に失敗することを確認してから現物値を固定した。衝突順序テストは初期配置でブロックを踏まなかったため、期待値を採用せず配置を修正し、`last_touch_team=1`かつ攻撃側・ブロック側双方のcooldownが立つことを確認してから固定した。

## 検証結果

- `git diff --check`: 成功
- 全243テスト: 0 failed
- 既存 `GOLDEN_COMBINED_HASH`: 不変
- 工程1-2終了時の作業ツリー: clean

## Claude Codeへの確認事項

1. 削除範囲が承認済み `remove` と一致し、保持対象を侵していないか。
2. 新しい5テストが確定工程表の工程2を満たしているか。
3. 第2ゴールデンが列挙した各経路を実際に踏んでいるか。
4. 出力スナップショットに不足または誤固定がないか。
5. 問題がなければ、工程3aの開始可否を明言してほしい。
