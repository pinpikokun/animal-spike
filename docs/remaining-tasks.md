# 残タスク（現役）

最終更新: 2026-07-29

P0 = いま手を動かしているものと実害のあるバグ、P1 = 土台（コードが増える前に直す）、
P2 = ゲームの中身。P3 以降は `docs/tasks-backlog.md`、経緯は `docs/archive/`。
各タスクの根拠と設計の詳細は `docs/tasks/<番号>.md` にある。

| | 番号 | やること |
|---|---|---|
| P0 | #100 | **大部分完了・再決定待ち。** 固定速度化後もサーブ線14pxはジャンプCで不成立 |
| P0 | #66 | CPU位置取り再設計 |
| P1 | #86 | **完了。** 決定論乱数のハッシュ実装が2種類ある＋使われないsaltがある |
| P1 | #88 | **完了（4段すべて）。** 決定論乱数をステートフル方式へ移行する |
| P1 | #105 | **完了。** 乱数の種が実ゲームで配線されていない |
| P1 | #106 | **完了。** 乱数2ワードの所有者をプロセスへ移し再戦で継承する |
| P1 | #108 | テストランナーの偽PASS防御に穴がある |
| P1 | #109 | 表示層のパース検査が空振りしている（新規・急がない） |
| P1 | #110 | `src/net` のパース検査能力を確認する（新規・急がない） |
| P1 | #111 | 低速PCのロールバック上限とsnapshot最適化 |
| P2 | #101 | 味方へ上げるトスの高さに駆け引きを作る |
| P2 | #80 | 味方CPUが全部自分でやり人間の出番がない |
| P2 | #104 | 前衛CPUがブロックとアタック返しを使い分ける |
| P2 | #70 | 操作体系の原作準拠化 |
| P2 | #84 | CPUの狙い誤差・ミス抽選が原作方針と矛盾している |
| P2 | #76 | 打撃モデルを「構え式」へ作り直す |
| P2 | #96 | 暫定値の実測確定 |
| P2 | #107 | 共有シード・Seeded Challenge の遊び方を検討する |

---

# P0 — 今やる（2件）

## 0. 【完了】足場作り（2026-07-28 コミット `8db6eff`。プッシュ済み）

**433 tests, 0 failed。作業ツリーはクリーン。** 以降の作業は緑の足場から始められる。

入ったもの: #102 スパイク横速度の固定化／CPUサーブ打点の上昇／CPU反応遅延の
原作準拠化／書類整理。**#82 は足場から外した**（`git show 8db6eff` の直前の状態に
実装が残っている。再設計は #88 の後）。

経緯と、この作業で分かったことは `docs/tasks/p0-note-1.md`。
設計書は `docs/superpowers/specs/2026-07-27-scaffold-isolate-82.md`。

## 着手順の決定「A改」（Claude Code と Codex が4往復して合意。2026-07-27）

**#82 には直接入らない。#86 → #88 を先に通してから戻る。**

1. **#82 の差分だけを足場から外し、完成している仕事をコミットして緑の足場を作る**
   理由: 乱数へ触る間、既知の赤を抱えたままだと、新しい赤が #82 由来か #86/#88 由来か
   判別できず、**フルテストが警報器として死ぬ**（Codex の指摘）。
   設計書 `docs/superpowers/specs/2026-07-27-scaffold-isolate-82.md`

   **手段は「打ち消し方式」。`git stash` は使わない**（staged 側に docs の rename が
   30件以上あり、stash して戻すと rename 検出が壊れる）。封印は作業ディレクトリ外への
   退避で果たす。
   **合格条件は、`GOLDEN_COMBINED_HASH` を張り替えずに `1173764543217565771` のまま
   全件緑になること。** この値は #102 と反応遅延の分で既に更新済みなので、
   #82 を完全に剥がせば戻るはず。**張り替えが要るなら手術が不完全な証拠**として扱う。
2. **#86 を挙動不変で通す**（乱数値を1つも変えない。ゴールデン張り替え無し）
   → 緑の足場を作ってから乱数へ触る
3. **#88 で乱数を原作方式のステートフルへ移行。** 大きすぎるので4段に割った
   （Claude Code と Codex が合議。**原因を切り分けられなくなるのが最大の危険**で、
   ゴールデン再採取の回数を減らすことより切り分けを優先する）

   | 段 | 中身 | ゴールデン再採取 | 状態 |
   |---|---|---|---|
   | **#88a** | PRNG と状態(`rng`/`aitick`)だけ。既存抽選は読まない | 1回 | **完了 `5ce8d4d`** |
   | **#88b-1** | 役割抽選を新方式へ。**役割を `SimState` に保存** | 1回 | **完了 `287d93b`** |
   | **#88b-2** | 残りの抽選の乱数源切り替えと派生値方式 | 1回 | **完了** |
   | **#88b-3** | テスト三層への全体整理。`SALT_RECEIVE` 依存6本の是正 | なし | **完了 `dacba8e` + `19b772c`** |

   **#88 は4段すべて完了した。次は #105（種の配線）→ #82 の設計書書き直し。**

   **#88b-1 で分かったこと**: テストから `rally_seq` の探索が全て消えた。
   「その条件が成立する種を1000回探す」書き方は、**本体の仕組みが変わった瞬間に
   意味を失ったまま緑で居座る**。ゲートで1本が赤として出たので直し、あわせて
   **赤にすらならない偽の前提**（`test_controls_original.gd`）も撤去した。
   `SALT_RECEIVE` 依存6本が同じ形で残っており、これは #88b-3 の担当。

   **#88b-2 の設計書査読で分かったこと**: その `SALT_RECEIVE` 依存は、
   **すでに偽の緑になっていた**。`test_cpu_offense_receive.gd` の
   `_select_successful_roll` は `SALT_RECEIVE`(値4) で成功する種を探して
   `last_hit_tick` へ入れるが、**本体が引くのは `SALT_SWEET`(値3)**。
   別の salt の出目を流用している。

   実測（Godot で `_noise` を直接叩いた）: テストが使う `PRESET_MAX` の
   `actor=1` で、選ばれた種の `SALT_SWEET` の出目は **227**。しきい値
   `P_SWEET=191` を超えているので**失敗**する。「ジャストレシーブが成功する
   状態を作る」というヘルパーの目的は果たされていない。**それでもテストは緑。**
   `PRESET_NORMAL` にいたっては `SALT_RECEIVE` で成功する種が 900 個中に1つも無い。

   **これが厄介なのは、#88b-2 で `SALT_SWEET` を `aitick` へ移しても赤にならない点**。
   元から効いていないので、「赤くなったら停止する」という検出では捕まらない。
   #88b-1 の `rally_seq` は赤で捕まえられたが、こちらは捕まらない。
   #88b-3 でヘルパーを正しく直した瞬間に初めて `_sweet_ok` が効き始め、
   テストが赤くなる可能性がある。**それは真の検査が復活したということ**なので、
   期待値を緩めて通してはいけない。

   **#88b-2 で分かったこと**: ゴールデンの数え方が間違っていた。

   設計書は状態ハッシュ固定検査群を2本（`test_golden_hash_regression` と
   `test_hit_chain_second_golden`）と定義したが、**実際に動いたのは前者だけ**だった。
   後者の `_world()` は `SimState.new()` だけを使い `Simulation.tick()` を通らないので、
   全過程で `tick=0 / rng=0 / aitick=0` のまま動く。旧 `keyed_hash(s.tick=0, ..)` と
   新 `keyed_hash(aitick=0, ..)` が同じ値になるため、**乱数源の変更を原理的に検出できない**。
   動いていない値を体裁のために書き換えることはせず、据え置いた。

   代わりに、設計書が群へ入れそこねた
   `test_refactor_characterization.gd.test_scatter_stream_snapshot` が捉えていた。
   こちらは乱数列を直接固定するテストで、実質的にゴールデンと同じ役割を持つ。

   もう1つ、**`s.tick` を回して乱数を散らすテストが4本**あり、乱数源が `aitick` へ
   移った瞬間に「1種類しか出ない」状態になった（60個の乱数列が12個の繰り返しに、
   100種類のロール値が1種類に）。前提を `s.aitick` へ移すだけで直り、
   **`[10, 80, 10]` や「ちょうど30%」といった期待値は1文字も変えていない**。
   `aitick` を回しても同じ分布が出ることを、実装前に独立計算で確認してから承認した。

   **#88b-3 で分かったこと**: 第1段階（`dacba8e`）で、下に挙げた残り3件をすべて
   是正した。`_select_successful_roll` と `_aitick_for_mura_pct` は消え、900回探索も
   消えた。`test_hit_chain_second_golden` は
   `test_hit_chain_physics_state_transition_golden` へ改名し、乱数源を責務外と
   明記した（**乱数源の変更を原理的に検出できないテストに、検出役の名前が
   ついていたのが問題だった**）。第2段階（`19b772c`）は参照ゼロになった
   `_noise()` / `_roll()` と旧 salt 4個の撤去だけで、固定値は1つも動いていない。

   **#88b-3 で片付けた残り**（いずれも緑だが健全性の証拠になっていなかった）:

   - `SALT_RECEIVE` 依存6本。`_select_successful_roll` が別 salt の出目を流用しており、
     すでに偽の緑（コミット `6ceff0a`）。`aitick` の直接指定へ移せば900個の探索は消える
   - `test_hit.gd` に残る `s.tick = ` 3箇所（384 / 421 / 458 行）。
     384 は `_scatter` を、458 は `SALT_TOSS_BAD` を `s.tick` で振ろうとしているが
     もう効かない。とくに 458 の `test_only_toss_bad_can_produce_low_toss` は
     `check(upward <= bump_up_speed)` という上限確認なので、**抽選が効かなくても通る**
   - `test_hit_chain_second_golden` が乱数を検出できない件。三層のどこへ置くかを決める

   **各段の契約テストは実装より先に書く**（Codex の指摘。古い書き方のテストで
   新方式を検証する状態を作らないため）。三層への全体整理だけを最後に置く。

   **#88b-1 を独立させた理由**: 他は「乱数源を差し替える」だけだが、役割抽選は
   **役割を状態として持つ構造変更**を伴い、しかも**原作で唯一 `rng` を進める例外**。
   混ぜると挙動が動いた原因を切り分けられない。

4. **#88 の後に #82 の設計書を書き直して再承認** → 幅判定とブロック2本を含めて緑へ
   （ブロック2本の真因はテストの前提バグと判明済み。`docs/tasks/p0-note-1.md`）

**#82 の乱数キーだけ暫定で直す案（B）は却下。** #82 の設計書は乱数の作りに
依存しており、方式が変われば書き直しになる。同じ設計書を2回書いて2回承認する費用の方が重い。

## 1. #100 ネットを越えて相手コートへ返す球を作り直す（大部分完了）

固定速度の `opponent_return_vx()`、全打点・高さ・特性・入射の網羅保証、自陣トス
248/136px、地上と空中の3打目強制は実装済み（`cb57141`、`4bd794b`、`7602485`）。
`serve_line_px` 14pxは、スパイク固定速度化後もジャンプCのアタックサーブが左右とも
ネットを越えず、承認設計の前提が不成立と判明した。31pxへ復元済み。ジャンプA/B/C/Eの
実通過検査は490件の正規入口で緑だが、Eは31pxでも上端余裕が約0.05pxしかない。
打点・速度・照準を変更せず、14pxを撤回するか別方式にするかをユーザーが再決定する。

サーブ（トスサーブ）と3打目の返球を、原作と同じ「速度を直接決める」方式にする。
詳細: `docs/tasks/100.md`

## 2. #66 CPU位置取り再設計（団子・味方妨害・人間の出番奪いを一括）

味方CPU同士が同じ球へ突っ込んで3タッチを超えて失点する、2体が団子になる、味方CPUが全部自分でやって人間の出番が無い——この3つを一括で直す。
詳細: `docs/tasks/66.md`

# P1 — 土台。コードが増える前に（5件）

## 1. #86 決定論乱数のハッシュ実装が2種類ある＋使われないsaltがある

同じ役目のハッシュ関数が2つあり、式が違う。
詳細: `docs/tasks/86.md`

## 2. #88 決定論乱数をステートフル方式へ移行する（設計ミスの是正）

いまの乱数は「状態を持たず、キーから毎回計算する」方式。
詳細: `docs/tasks/88.md`

## 3. 【完了】#105 乱数の種が実ゲームで配線されていない（`9b71986` + `0561153`）

`reset_match` は #88a で `seed` 引数を持ったが、実ゲームの呼び出し3箇所すべてが
省略しており、**必ず `rng = aitick = 0` で始まる**。原作は起動時に DOS 時刻を
1回だけ種にする機構を持っているので、原作準拠の既定に反する。

**範囲は「起動時の種の配線」だけ。固定値は1つも動いていない**（宣言どおり）。
再戦での系列継承は #106 へ分離した（Claude Code と Codex が3往復して合意）。

**実測**: `run_tests.ps1` exit 0 / 461 tests, 0 failed / SCRIPT ERROR 0。
`run_net_test.ps1 -VerifyBootSeed` exit 0。seed=2590（`0x0A1E` = 10時30分）が
実行時刻と一致し、host/join の seed・roster・serving・初期 hash が一致、mismatch 0。

**#105 で分かったこと**（設計書を3回差し戻し、実装を2回赤で突き返した）:

- **`_init()` では `Engine.get_main_loop()` が null、`_initialize()` では
  `root.is_inside_tree()` が false。** どちらも子ノードの `_ready()` が発火しない。
  最初の `_process()` で初めて同期発火する。テスト実行本体をそこへ移した
- **`461 tests, 0 failed` と表示されながら SCRIPT ERROR を出す偽の緑を踏んだ。**
  事前ガードの `check()` が先に通ると `checks_run == 0` 防御が効かない。
  救ったのは `run_tests.ps1` の SCRIPT ERROR 検査（二段目の防御）。→ #108
- **合否の正規の入口は `run_tests.ps1` の終了コード。** Godot 単体が
  `461 tests, 0 failed` / exit 0 でも、ps1 が赤なら不合格。今回まさにそこが分かれ目になった
- 変異検査3件で、テストが空振りしていないことを確認済み

**未達**: キャラ選択を人手で進める実ゲーム確認（`VERIFY-005`）。
ヘッドレス起動のスモークとコードレビューまでは済んでいる。

詳細: `docs/tasks/105.md`

## 4. 【完了】#106 乱数2ワードの所有者をプロセスへ移す（`f5be60a` + `2dc97fa`）

原作は `9c00` で2ワードを1回だけ初期化し、試合・セット・ラリーのどこにも再初期化が無い。
本作は `reset_match` が毎試合これを書いており、ローカル再戦が起動時の種へ巻き戻っていた。

    reset_match(s, cfg, serving_team, roster, rng_word, aitick_word)   末尾3つに既定値なし

**「所有者を移す」は「代入を外す」ではない。** 所有とは値を決めて供給する責任であって
代入の実行者ではない。`reset_rally` が開始時に rng を2回消費するため、呼ぶ前に state へ
暗黙に仕込む方式は危険。代入を外す案は実測して捨てた（461本中2本しか赤くならないが、
`0` は正当値なので「安い」ではなく「暗黙の入力が46箇所で黙って通る」という意味だった）。

`root.gd` がプロセスの器として起動時に1回だけ2ワードを持ち、再戦では破棄直前の
`GameView.state` から回収して次へ渡す。試合中は待機2ワードを双方 `null` に保つ。

検証: `462 tests, 0 failed` / exit 0 / SCRIPT ERROR 0、呼び出し57件すべて6引数、
固定値の変更0件、変異3件（回収削除・boot_seed再開・rng/aitick入替）がすべて赤。

**副産物として #109 を発見した。** `root.gd` のパース検査除外を撤去したが、
`test_display_scripts_parse` はパースエラーでも赤くならない空振り検査だった。

## 5. #108 テストランナーの偽PASS防御に穴がある（急がない）

**SCRIPT ERROR を出しているテストが PASS と表示される。** `checks_run == 0` 防御は
「1回も check していないテスト」しか捕まえられず、**事前ガードの check が先に通って
から本命がランタイムエラーで死ぬ形は素通りする**。#105 の実装中に実測（Godot単体は
`461 tests, 0 failed` / exit 0、`run_tests.ps1` は SCRIPT ERROR 検査で exit 1）。
二段目の防御が効いているので実害は限定的だが、**「緑は健全さの証拠ではない」という
本プロジェクトの中心テーマそのもの**なので放置しない。
詳細: `docs/tasks/108.md`

## 6. #109 表示層のパース検査が空振りしている（急がない）

**`test_display_parse.gd` は表示層がパースエラーでも赤くならない。**
`load()` はパース失敗でも非 null の `GDScript` を返すので `check(res != null)` が
常に真になる。#106 の変異検査中に実測（`root.gd` と `game_view.gd` の双方で
`Failed to load script ... Parse error` が出ているのに `PASS`）。

**この検査の存在理由は「表示層のパースエラーがテスト全緑のままゲームだけ壊す事故が
実際に起きた」ことだった。その事故を防ぐために作った検査が防いでいない。**
#108 と同じ構造だが、#108 は二段目（`run_tests.ps1`）が実際に止めたのに対し、
**こちらは二段目も止めていない**（`Failed to load script` を見ていない）。
実際に赤くしたのは #106 で追加した結合テストの方だった。
詳細: `docs/tasks/109.md`

## 7. #111 低速PCのロールバック上限とsnapshot最適化

#83は基準機での大幅な退行を止めるが、3〜4倍遅いPCの16コマ再計算は
半フレーム予算を超え得る。SyncManager固定費を含む実機測定、許容巻き戻し数、
snapshot保存の最適化を別課題で確定する。
詳細: `docs/tasks/111.md`

# P2 — ゲームの中身。遊んだときの面白さ（8件）

## 0. #101 味方へ上げるトスの高さに駆け引きを作る（課題提起のみ・未設計）

現実のバレーでは、高いトスは打ちやすいが相手にタイミングを読まれてブロックされ、低い速攻は打つのが難しいが読まれない、という交換が成立している。
詳細: `docs/tasks/101.md`

## 1. #80 味方CPUが全部自分でやり人間の出番がない

味方CPUが自分でレシーブして自分で前に出て自分でアタックまで完結してしまう。
詳細: `docs/tasks/80.md`

## 2. #104 前衛CPUがブロックとアタック返しを使い分ける（本作独自）

飛んでくるアタックに対し、前衛のCPUが「ブロック」と「アタック返し」を状況で使い分ける。
詳細: `docs/tasks/104.md`

## 3. #70 操作体系の原作準拠化（空中の上キーをアタックへ／ブロックを前キーへ）

空中の上＋ボタンが原作ではアタック、現行ではブロックになっている。
詳細: `docs/tasks/70.md`

## 4. #84 CPUの狙い誤差・ミス抽選が原作方針と矛盾している

原作資料は「狙いにノイズを注入する箇所は一切ない」「わざとミスするロジックは存在しない」と結論し、弱さは反応遅延と発火間引きだけで作れと書いている。
詳細: `docs/tasks/84.md`

## 5. #76 打撃モデルを「構え式」へ作り直す（押し打ち/離し打ちの土台）

いまの打撃は「押した瞬間に判定」だが、これを「構えて離す」モデルへ作り直す。
詳細: `docs/tasks/76.md`

## 6. #96 暫定値の実測確定（バーンアウト10秒・スズメ/馬のパワー）

バーンアウト復帰10秒は「KPIハーネスで平均ラリー時間を実測してから最終決定」と仕様書に書いたまま未実測。
詳細: `docs/tasks/96.md`

## 7. #107 共有シード・Seeded Challenge の遊び方を検討する（課題提起のみ・未設計）

**実装タスクではない。「競技用／練習用／バグ再現用のどれに使うか」をユーザーへ問う
設計質問から始まる。** Slay the Spire の Daily Climb、Spelunky の Daily Challenge の
ように、種を表向きにして同じ条件で競う遊び方は本作でも成立しそうで、#96（KPIハーネス）
とも繋がる。**種だけでは足りず**、ルールのバージョン・ロスター・CPUプロファイル・
サーブ側・採点方法・挑戦回数を揃えて初めて「全員同じ条件」になる。
詳細: `docs/tasks/107.md`
