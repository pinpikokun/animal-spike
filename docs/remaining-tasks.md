# 残タスク（現役）

最終更新: 2026-07-27

P0 = いま手を動かしているものと実害のあるバグ、P1 = 土台（コードが増える前に直す）、
P2 = ゲームの中身。P3 以降は `docs/tasks-backlog.md`、経緯は `docs/archive/`。
各タスクの根拠と設計の詳細は `docs/tasks/<番号>.md` にある。

| | 番号 | やること |
|---|---|---|
| P0 | — | 【未コミット・作業中】2026-07-26〜27 の変更 |
| P0 | #82 | CPUの打ち方の選び方を原作の仮想打球テストへ作り直す |
| P0 | #100 | ネットを越えて相手コートへ返す球を作り直す |
| P0 | #66 | CPU位置取り再設計 |
| P0 | #91 | ラリー再開で消えない状態＋ネット経路の設定検査漏れ |
| P0 | #83 | 性能の見張りテスト |
| P1 | #86 | 決定論乱数のハッシュ実装が2種類ある＋使われないsaltがある |
| P1 | #88 | 決定論乱数をステートフル方式へ移行する |
| P2 | #101 | 味方へ上げるトスの高さに駆け引きを作る |
| P2 | #80 | 味方CPUが全部自分でやり人間の出番がない |
| P2 | #104 | 前衛CPUがブロックとアタック返しを使い分ける |
| P2 | #70 | 操作体系の原作準拠化 |
| P2 | #85 | CPUのジャンプ発火条件を原作のAND判定へ絞る |
| P2 | #84 | CPUの狙い誤差・ミス抽選が原作方針と矛盾している |
| P2 | #76 | 打撃モデルを「構え式」へ作り直す |
| P2 | #96 | 暫定値の実測確定 |

---

# P0 — 今やる（5件）

## 0. 【未コミット・作業中】2026-07-26〜27 の変更（5件目が難航中）

未コミットのゲームコード変更あり。テストは456本中6本が赤（2026-07-27 実測）。
書類整理(CLAUDE.md/AGENTS.md/docs 一式)もステージ済みだが、pre-commit が赤を弾くため
まだ入っていない。**#82 を直して全件緑にしてから、まとめてコミットする。**

**消してはいけない git 未追跡ファイル**（`git clean -fd` 厳禁）:
`tests/unit/test_cpu_trial_shot.gd` — #82 の検証テスト。赤2本を含む本体。
（`2026-07-26-cpu-trial-shot-design.md` は追加済みで安全。以前の記述は逆だった）

**`data/rules.json` は未ステージ**。`spike_vx_px_s` 680→2720、新キー `spike_mid_vx_px_s`
を追加してある。`src/sim/sim_config.gd:153` がこの新キーを必須で読むため、
**ステージし忘れてコミットすると起動しなくなる。**

詳細: `docs/tasks/p0-note-1.md`

## 着手順の決定「A改」（Claude Code と Codex が4往復して合意。2026-07-27）

**#82 には直接入らない。#86 → #88 を先に通してから戻る。**

1. **#82 の差分だけを足場から外し、完成している仕事をコミットして緑の足場を作る**
   理由: 乱数へ触る間、既知の赤を抱えたままだと、新しい赤が #82 由来か #86/#88 由来か
   判別できず、**フルテストが警報器として死ぬ**（Codex の指摘）。
   設計書 `docs/superpowers/specs/2026-07-27-scaffold-isolate-82.md`

   **手段は「打ち消し方式」。`git stash` は使わない**（staged 側に docs の rename が
   30件以上あり、stash して戻すと rename 検出が壊れる）。封印は作業ディレクトリ外への
   退避で果たす。
   **合格条件は、`GOLDEN_COMBINED_HASH` を張り替えずに `1173764543217565771` のまま
   全件緑になること。** この値は #102 と反応遅延の分で既に更新済みなので、
   #82 を完全に剥がせば戻るはず。**張り替えが要るなら手術が不完全な証拠**として扱う。
2. **#86 を挙動不変で通す**（乱数値を1つも変えない。ゴールデン張り替え無し）
   → 緑の足場を作ってから乱数へ触る
3. **#88 で乱数をステートフルへ移行。** 更新周期・初期状態・消費順・保存復元・
   同期ハッシュ対象・**単一系列か用途別系列か**を契約に固定する。
   **ゴールデンの張り替えはここで1回だけ**
4. **テストを三層へ作り直す**（PRNG単体／抽選単体／シナリオ）
5. **#88 の後に #82 の設計書を書き直して再承認** → 幅判定とブロック2本を含めて緑へ

**#82 の乱数キーだけ暫定で直す案（B）は却下。** #82 の設計書は乱数の作りに
依存しており、方式が変われば書き直しになる。同じ設計書を2回書いて2回承認する費用の方が重い。

## 1. #82 CPUの打ち方の選び方を原作の仮想打球テストへ作り直す（設計完了・実装直前）

CPUが空中で打つとき、打つ前に頭の中で試して「相手コートに入るか」で選ぶ。
詳細: `docs/tasks/82.md`

## 2. #100 ネットを越えて相手コートへ返す球を作り直す（設計完了・実装未着手）

サーブ（トスサーブ）と3打目の返球を、原作と同じ「速度を直接決める」方式にする。
詳細: `docs/tasks/100.md`

## 3. #66 CPU位置取り再設計（団子・味方妨害・人間の出番奪いを一括）

味方CPU同士が同じ球へ突っ込んで3タッチを超えて失点する、2体が団子になる、味方CPUが全部自分でやって人間の出番が無い——この3つを一括で直す。
詳細: `docs/tasks/66.md`

## 4. #91 ラリー再開で消えない状態＋ネット経路の設定検査漏れ

`reset_rally`がstun/burn/dive/brakeなど多くをクリアする一方、push（吹っ飛び速度）/dash/tap_dir/tap_tickをクリアしていない。
詳細: `docs/tasks/91.md`

## 5. #83 性能の見張りテスト（1コマのコスト上限）

1コマのシミュレーションのコストが決めた上限を超えたらテストが落ちる仕組み。
詳細: `docs/tasks/83.md`

# P1 — 土台。コードが増える前に（2件）

## 1. #86 決定論乱数のハッシュ実装が2種類ある＋使われないsaltがある

同じ役目のハッシュ関数が2つあり、式が違う。
詳細: `docs/tasks/86.md`

## 2. #88 決定論乱数をステートフル方式へ移行する（設計ミスの是正）

いまの乱数は「状態を持たず、キーから毎回計算する」方式。
詳細: `docs/tasks/88.md`

# P2 — ゲームの中身。遊んだときの面白さ（8件）

## 0. #101 味方へ上げるトスの高さに駆け引きを作る（課題提起のみ・未設計）

現実のバレーでは、高いトスは打ちやすいが相手にタイミングを読まれてブロックされ、低い速攻は打つのが難しいが読まれない、という交換が成立している。
詳細: `docs/tasks/101.md`

## 1. #80 味方CPUが全部自分でやり人間の出番がない

味方CPUが自分でレシーブして自分で前に出て自分でアタックまで完結してしまう。
詳細: `docs/tasks/80.md`

## 2. #104 前衛CPUがブロックとアタック返しを使い分ける（本作独自）

飛んでくるアタックに対し、前衛のCPUが「ブロック」と「アタック返し」を状況で使い分ける。
詳細: `docs/tasks/104.md`

## 3. #70 操作体系の原作準拠化（空中の上キーをアタックへ／ブロックを前キーへ）

空中の上＋ボタンが原作ではアタック、現行ではブロックになっている。
詳細: `docs/tasks/70.md`

## 5. #85 CPUのジャンプ発火条件を原作のAND判定へ絞る

原作は「頂点付近AND横に流れていないAND十分高い」のANDで跳ぶ球を選び、悪い球には跳ばない。
詳細: `docs/tasks/85.md`

## 6. #84 CPUの狙い誤差・ミス抽選が原作方針と矛盾している

原作資料は「狙いにノイズを注入する箇所は一切ない」「わざとミスするロジックは存在しない」と結論し、弱さは反応遅延と発火間引きだけで作れと書いている。
詳細: `docs/tasks/84.md`

## 7. #76 打撃モデルを「構え式」へ作り直す（押し打ち/離し打ちの土台）

いまの打撃は「押した瞬間に判定」だが、これを「構えて離す」モデルへ作り直す。
詳細: `docs/tasks/76.md`

## 8. #96 暫定値の実測確定（バーンアウト10秒・スズメ/馬のパワー）

バーンアウト復帰10秒は「KPIハーネスで平均ラリー時間を実測してから最終決定」と仕様書に書いたまま未実測。
詳細: `docs/tasks/96.md`
